#!/usr/bin/env bash
# .automated_script.sh — runs from the live environment on boot
# Override in your edition to add custom first-boot behavior.

# Example: launch a terminal for manual installation
# /usr/bin/xterm -e /usr/local/bin/Installation_guide
